menu hshift 9
menu width 58

label amd64-expert
	menu label 64 bit expert install
	kernel ${AMD_KERNEL}
	append priority=low ${VIDEO_MODE} initrd=${AMD_INITRD} -- ${CONSOLE}
include ${SYSDIR}amdrqtxt.cfg
