MEDIUM_SUPPORTED = netboot cdrom

# The version of the kernel to use.
KERNELVERSION := $(KERNELVERSION)-omap
# we use non-versioned filenames in the omap kernel udeb
KERNELNAME = vmlinuz
VERSIONED_SYSTEM_MAP =
