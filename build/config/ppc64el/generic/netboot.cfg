MEDIA_TYPE = netboot image

TARGET = $(INITRD) $(KERNEL)
EXTRANAME = $(MEDIUM)/

MANIFEST-INITRD = "initrd for Little-Endian 64-bit POWER CPUs (POWER8 and later)"
MANIFEST-KERNEL = "kernel for Little-Endian 64-bit POWER CPUs (POWER8 and later)"
