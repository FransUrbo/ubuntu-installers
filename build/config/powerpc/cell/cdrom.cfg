MEDIA_TYPE = CD-ROM

# cd booting does not need floppy images on powerpc
TARGET = $(INITRD) $(KERNEL) builtin_initrd
EXTRANAME = $(MEDIUM)/

MANIFEST-BOOT = "CDROM image for Cell cpus"
MANIFEST-INITRD = "initrd for use with Cell cpus"
MANIFEST-KERNEL = "kernel for use with Cell cpus"
