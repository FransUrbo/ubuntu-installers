MEDIA_TYPE = netboot image

TARGET = $(INITRD) $(KERNEL) $(MINIISO) builtin_initrd netboot_content
EXTRANAME = $(MEDIUM)/

MANIFEST-BOOT = "tftp boot image for Cell cpus"
MANIFEST-INITRD = "initrd for Cell cpus"
MANIFEST-KERNEL = "kernel for Cell cpus"
MANIFEST-MINIISO = "small bootable CD image for Cell netboot"
