MEDIA_TYPE = netboot image

TARGET = $(INITRD) $(KERNEL)
EXTRANAME = $(MEDIUM)/

MANIFEST-BOOT = "tftp boot image for e500 PowerPC cpus"
MANIFEST-INITRD = "initrd for e500 PowerPC cpus"
MANIFEST-KERNEL = "kernel for e500 PowerPC cpus"
